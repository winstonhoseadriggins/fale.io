box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.13
        flags: --config=config-it.yaml --buildDrafts=true
    - arjen/hugo-build:
        version: 0.13
        flags: --config=config-en.yaml --buildDrafts=true
