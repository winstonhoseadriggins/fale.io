box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.13
        config: config-it.yml
        flags: --buildDrafts=true
    - arjen/hugo-build:
        version: 0.13
        config: config-en.yml
        flags: --buildDrafts=true
