box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.13
        config: config-it.yaml
        flags: --buildDrafts=true
    - arjen/hugo-build:
        version: 0.13
        config: config-en.yaml
        flags: --buildDrafts=true
